<div class="row" style="width: 100%">
  <h2>Chart of Accounts</h2>
</div>
<div class="row">
  <div class="row" style="width: 100%">
    Bank Accounts | 
    <%= link_to 'New', new_bank_account_path %>
  </div>
  <div class="row">
    <table>
      <thead>
        <tr>
          <th>Name</th>          
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @bank_accounts.each do |bank_account| %>
          <tr>
            <td><%= bank_account.name%></td>            
            <td><%= link_to 'Show', bank_account %></td>
            <td><%= link_to 'Edit', edit_bank_account_path(bank_account) %></td>
            <td><%= link_to 'Destroy', bank_account, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<div class="row">
  <div class="row" style="width: 100%">
    Wishlist Accounts | 
    <%= link_to 'New', new_wishlist_account_path %>
  </div>
  <div class="row">
    <table>
      <thead>
        <tr>
          <th>Name</th>          
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @wishlist_accounts.each do |wishlist_account| %>
          <tr>
            <td><%= wishlist_account.name%></td>            
            <td><%= link_to 'Show', wishlist_account %></td>
            <td><%= link_to 'Edit', edit_wishlist_account_path(wishlist_account) %></td>
            <td><%= link_to 'Destroy', wishlist_account, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
