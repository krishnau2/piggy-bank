<h1>Bank Accounts</h1> <%= link_to 'Create', new_bank_account_path %>
<% @bank_accounts.each do |account| %>
  <div>
    <h2><%= account.name %></h2>
    <h4><%= account.amount %></h4>
  </div>
<% end %>

<hr /> 

<h1>Wishlists</h1>
<% @wishlist_accounts.each do |account| %>
  <div>
    <h2><%= account.name %></h2>
    <h4><%= account.amount %></h4>
  </div>  
<% end %>